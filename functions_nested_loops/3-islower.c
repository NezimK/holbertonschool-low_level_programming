#include "main.h"
#include <ctype.h>

/**
 * _islower - function that checks for lowercase character'
 * @c: check
 * Return: (0).
 */

int _islower(int c)
{

	if (c >= 'a' && c <= 'z')


	return (1);

	return (0);
}
