#include <stdio.h>
#include "main.h"

/**
 * _isalpha - checks for alphabetic character.
 * @c : check
 */

int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'a') || (c >= 'A' && c <= 'Z'))

	return (1);

	return (0);
}
